---
title: "textToSpeechStream( )"
titleStyle: code
description: "Duplex session: write UTF-8 text fragments (e.g. LLM token deltas) via `write`. Each string or
Buffer should be a complete UTF-8 fragment. The worker forwards them to ONNX TTS `runStreaming`
(optional sentence accumulation via request fields). Iterate the session for `TextToSpeechStreamResponse`
lines (PCM in `buffer`, optional `chunkIndex` / `sentenceChunk`) until `done`."
---

```typescript
function textToSpeechStream(params: { accumulateSentences?: boolean; flushAfterMs?: number; inputType: string; maxBufferScalars?: number; modelId: string; sentenceDelimiterPreset?: "latin" | "cjk" | "multilingual" }, options?: { forceNewConnection?: boolean; healthCheckTimeout?: number; profiling?: { enabled?: boolean; includeServerBreakdown?: boolean; mode?: "summary" | "verbose" }; timeout?: number }): Promise
```

## Parameters

| Name | Type | Required? | Description |
| --- | --- | :---: | --- |
| `params` | `\{ accumulateSentences?: boolean; flushAfterMs?: number; inputType: string; maxBufferScalars?: number; modelId: string; sentenceDelimiterPreset?: "latin" | "cjk" | "multilingual" \}` | ✓ | No description |
| `options` | `\{ forceNewConnection?: boolean; healthCheckTimeout?: number; profiling?: \{ enabled?: boolean; includeServerBreakdown?: boolean; mode?: "summary" | "verbose" \}; timeout?: number \}` | ✗ | No description |

## Returns

```typescript
Promise
```